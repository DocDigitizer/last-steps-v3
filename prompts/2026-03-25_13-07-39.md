1. I think logistics are often tied with operations, and although it might make sense to have it as root, we want to disambiguate whenever we can, so let's put it under the Operations root. 
2. Let's keep that as a root to make it really simple and those rare documents from rare companies (rare is assumed) will get separated quickly.
