2. That is the flow. 

I do question this sentence "  - Classifier always reaches a leaf. Low confidence → on-the-fly schema generation, not intermediate-node fallback." , shouldn't we fallback to the general one instead of creating a schema on the fly? Since we have json fields in each node, the creation of a schema on the fly implies that our tree is not broad enough (which we know it's probably the reality of our situation in this first version of the tree) and we should change it. Btw, this is an important information that should be stored. The tree, when implemented need to be versioned so that we can adapt when we get better at this. 

3. The schema inheritance since it's controlled by us should be defined by this simple statement: "Is this field used in all child nodes? If yes then it belongs to that node, if not, it should be passed on to the specific children that use that field"

Does this make sense? Anything that doesn't make sense?
