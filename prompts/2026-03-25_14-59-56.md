Okay, about level 3. Do you think we can or should start adding leaf nodes? The goal of this project is at each leaf node to have a schema that the classifier will get to, after going down the tree. So, when should we start adding leaf nodes? The tree does not need to have the same height on every subtree. There are nodes that can be finished before others, there are no rules like that.
