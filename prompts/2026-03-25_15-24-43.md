1. First, in this case the document type and countries will fall and be deprecated because the document types will be the nodes itself, so no need to worry about that. 
2. The classifier will always choose a leaf node, if it's not confident it will say that and we will create a schema on the fly.
3. That is a great idea actually but we can have different schemas in the same node (by organization ID for example, there can be multiple organizations with an invoice.json schema, so each gets its own schema), however, for the public schemas that might be a really good idea. If you can, put that somewhere. It would solve problem 2 that you raised.
4. DocType+country will no longer be a thing. And the clients will load their schemas and we will attribute a classification. So that we can put them in the correct place in the tree.
5. One schema has one public schema and multiple private ones, otherwise it means that the node is not well classified and it should be split into to child nodes (do you see this?)

Let's keep talking and iterating over this. These are very important architecturally significant decisions.
